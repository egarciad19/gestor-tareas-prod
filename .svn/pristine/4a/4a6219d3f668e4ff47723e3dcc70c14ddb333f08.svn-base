import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CrearListaActividadComponent } from './crear-lista-actividad.component';

describe('CrearListaActividadComponent', () => {
  let component: CrearListaActividadComponent;
  let fixture: ComponentFixture<CrearListaActividadComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CrearListaActividadComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearListaActividadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
