import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TableroSeleccionadoComponent } from "./TableroSeleccionadoComponent";

describe('TableroSeleccionadoComponent', () => {
  let component: TableroSeleccionadoComponent;
  let fixture: ComponentFixture<TableroSeleccionadoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TableroSeleccionadoComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TableroSeleccionadoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
